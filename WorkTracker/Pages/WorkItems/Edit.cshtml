@page
@model WorkTracker.Pages.WorkItems.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>WorkItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WorkItem.Id" />
            <div class="form-group">
                <label asp-for="WorkItem.Title" class="control-label"></label>
                <input asp-for="WorkItem.Title" class="form-control" />
                <span asp-validation-for="WorkItem.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkItem.Description" class="control-label"></label>
                <input asp-for="WorkItem.Description" class="form-control" />
                <span asp-validation-for="WorkItem.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkItem.Created" class="control-label"></label>
                <input asp-for="WorkItem.Created" class="form-control" />
                <span asp-validation-for="WorkItem.Created" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkItem.Completed" class="control-label"></label>
                <input asp-for="WorkItem.Completed" class="form-control" />
                <span asp-validation-for="WorkItem.Completed" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkItem.StatusId" class="control-label"></label>
                <select asp-for="WorkItem.StatusId" class="form-control" asp-items="ViewBag.StatusId"></select>
                <span asp-validation-for="WorkItem.StatusId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkItem.PriorityId" class="control-label"></label>
                <select asp-for="WorkItem.PriorityId" class="form-control" asp-items="ViewBag.PriorityId"></select>
                <span asp-validation-for="WorkItem.PriorityId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkItem.ProjectId" class="control-label"></label>
                <select asp-for="WorkItem.ProjectId" class="form-control" asp-items="ViewBag.ProjectId"></select>
                <span asp-validation-for="WorkItem.ProjectId" class="text-danger"></span>
            </div>
            <div class="form-group">                
                <input type="submit" value="Save" class="btn btn-sm btn-info" />
                <a class="btn btn-sm btn-info" asp-page="./Index" asp-route-id="@Model.WorkItem.ProjectId">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
